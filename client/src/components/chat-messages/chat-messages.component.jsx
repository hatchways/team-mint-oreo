import React from 'react';
import { Box } from '@material-ui/core';

const ChatMessages = ({ height = 100 }) => {
  return (
    <Box>
      <Box>text</Box>
    </Box>
  );
};

export default ChatMessages;
