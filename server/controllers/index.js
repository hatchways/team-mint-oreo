const chatroom = require('./ChatroomController');
const invitation = require('./InvitationController');
const message = require('./MessageController');
const user = require('./UserController');

module.exports = {
  chatroom,
  invitation,
  message,
  user,
};
